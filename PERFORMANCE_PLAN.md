# 성능 개선 계획 및 구현 결과 (2026-08-13 계획 / 2026-08-14 구현)

안정성을 해치지 않으면서 추가로 올릴 수 있는 성능이 남아 있는지 핫패스 전체(수신 파이프라인,
송신 파이프라인, accept 경로, 세션 수명주기, 계측)를 코드 레벨로 재점검한 결과와,
그에 따라 실제로 구현한 내용을 정리한다.

## 결론 요약

**구조적 병목은 더 이상 없다.** 수신은 Pipelines 기반 zero-copy(필터가 `ReadOnlySequence`를
직접 파싱, 캐리 버퍼 없음), 송신은 세션당 single-flight + 배치 drain + scatter-gather
(`BufferList`), SAEA·풀 사전 할당, 요청당 힙 할당 0(라이브러리 구간 기준)까지 완료되어 있다.
관리형 SAEA + Pipelines 서버로서는 사실상 상한에 근접해 있다.

남은 것은 **2차 최적화**였고, 계획한 5개 항목(P1~P5)을 모두 구현했다.
그 과정에서 계획에 없던 **기존 버그 1건(송신 SAEA 풀 미반납)** 을 발견해 함께 고쳤는데,
성능 항목 전체를 합친 것보다 실질적 영향이 크다.

| 항목 | 종류 | 상태 | 결과 |
|---|---|---|---|
| **B1** 송신 SAEA 풀 미반납 (계획 외 발견) | 버그 수정 | **채택** | 서버가 평생 `MaxConnectionNumber`개까지만 접속받던 문제 해소 |
| P1 핫패스 공유 카운터 스트라이핑 | 코드 | **철회(게이트 미통과)** | 측정 결과 처리량 **-1.2%**. 아래 "측정" 참조 |
| P2 `ValidateClosed` 빠른 경로 | 코드 | 채택 | 송신 배치당 lock 1회 제거. 처리량은 노이즈 내 |
| P3 리스너 병렬 accept 루프 (opt-in) | 코드 | 채택 | `AcceptLoopCount` 기본 1 = 기존 동작 |
| P4 유휴 세션 zero-byte receive (opt-in) | 코드 | 채택 | `UseZeroByteReceive` 기본 false = 기존 동작 |
| P5 마이크로 묶음 | 코드 | 채택 | 연결당 할당 1개 제거, 로그 가드 |

**이번 작업에서 성능 수치를 끌어올린 항목은 없다.** 정상 부하에서 최종본은 기존 코드와
측정상 구분되지 않는다(아래 측정). 실질적인 성과는 B1 버그 수정과, P3·P4라는 두 개의
opt-in 안전밸브를 확보한 것이다.

검증: 라이브러리 빌드 **경고 0**, 회귀 테스트 **31건 → 40건 전부 통과**,
LoadTest 자체 테스트 **94/94 통과**, 전체 솔루션 빌드 오류 0.

```bash
# 검증 커맨드
dotnet build SuperSocketLite/SuperSocketLite.csproj -c Release
dotnet run --project Test/SuperSocketLiteRegressionTests -c Release
dotnet run --project Test/LoadTest/SuperSocketLite.LoadTest.Tests -c Release
```

---

## B1. 송신 SAEA가 풀로 돌아오지 않던 버그 (계획 외 발견)

P2의 검증 조건("종료 시 SAEA가 풀로 복귀하는지")을 테스트로 옮기다 발견했다.

### 증상

세션이 정상 종료해도 **송신 SAEA가 풀에 반납되지 않는다.** 수신 proxy는 반납되지만
송신 쪽만 누락된다. 결과적으로 서버는 **기동 후 누적 `MaxConnectionNumber`개**의 접속만
처리할 수 있고, 그 뒤로는 이미 모두 접속을 끊었더라도 새 접속을 전부 거부한다.
(`MaxConnectionNumber=4`로 순차 접속을 반복하면 정확히 4회차까지만 성공한다.)

### 원인

`AsyncSocketServer.SessionClosed`는 반납할 인스턴스를 `socketSession.SendSAEA`로 읽는데,
그 프로퍼티는 `_socketEventArgSend` 필드를 그대로 노출하고 있었다.
그런데 `AsyncSocketSession.OnClosed`는 **종료 핸들러를 부르기 전에** 그 필드를
`Interlocked.CompareExchange`로 null로 지운다(동시에 완료되는 송신이 다시 집어가지 못하게
하려는 것으로, 그 자체는 옳다). 따라서 핸들러가 읽는 시점에는 언제나 null이었다.

```csharp
// 종료 순서: 필드를 먼저 비우고 → 핸들러 실행
Interlocked.CompareExchange(ref _socketEventArgSend, null, sae);
...
base.OnClosed(reason);   // 여기서 SessionClosed가 SendSAEA를 읽지만 이미 null
```

### 수정

떼어낸 인스턴스를 `_detachedSendSAEA`에 보관하고 `SendSAEA`가 그것을 반환하게 했다.
살아있는 세션에서는 null이고, 종료 절차의 일부로만 공개된다 — 즉 "동시 송신이 집어가지
못하게 한다"는 원래 의도를 유지하면서 풀 반납만 가능해진다.
풀에서 온 인스턴스일 때만 공개하므로, 핸들러는 출처를 확인하지 않고 반납해도 된다.

### 검증

- 회귀 테스트 `A closed session returns both of its pooled SocketAsyncEventArgs`:
  `MaxConnectionNumber=4`로 **16회 순차 접속**하며 매 라운드 세션이 완전히 사라지는 것을
  기다린 뒤 다음 접속을 한다. 거부가 1건이라도 있으면 실패한다.
- 역검증: 수정 한 줄을 되돌리면 이 테스트와 P2 스트레스 테스트가 **둘 다 실패**하고,
  복원하면 둘 다 통과한다.

---

## P1. 핫패스 공유 카운터 스트라이핑 — **철회**

### 구현했던 내용

`StripedLongCounter`(internal, 코어별 셀 + 64바이트 간격)를 만들어 패킷마다 갱신되는 3개
(`_totalBytesReceived`, `_totalBytesSent`, `_totalHandledRequests`)를 교체했다. 공개 프로퍼티
시그니처는 유지했다. 동작은 정확했다 — 8스레드 × 25,000회 동시 `Add` 후 합이 정확히 일치했고,
실제 에코에서도 바이트·요청 수가 정확히 맞았다.

### 철회 이유

측정에서 **일관되게 처리량을 1.2% 떨어뜨렸다.** 두 번의 교차 측정이 원인을 P1로 특정한다.

- 1차(HEAD vs 전체 변경): 13,347 → 13,185 pps. 두 그룹의 3회 범위가 **겹치지 않음**.
- 2차(HEAD vs P1만 뺀 변경): 13,262 → 13,248 pps. 범위가 **완전히 중첩** = 차이 없음.

전제가 성립하지 않았다. 스트라이핑은 캐시라인 핑퐁이 지배적일 때 이득인데, 이 부하는
13,000 pps × 카운터 3개 ≈ 초당 4만 회 갱신을 16코어가 나눠 하는 수준이라 경합이 거의 없다.
그 상태에서는 `Thread.GetCurrentProcessorId()` + 배열 인덱싱 비용만 갱신마다 그대로 더해진다.

계획서의 채택 게이트("pps가 유의미하게 개선될 때만 채택, 아니면 되돌린다")에 따라
클래스와 전용 단위 테스트를 함께 제거했다. 카운터 정확성 테스트는 구현과 무관하게 가치가
있으므로 이름을 바꿔 남겼다(`Server byte and request totals stay exact under concurrent updates`).

**다시 볼 조건**: 전용 서버 장비에서 이 장비가 낼 수 없는 훨씬 높은 패킷 레이트(수십만 pps)가
실측될 때. 그 전까지는 근거 없는 최적화다.

---

## P2. `ValidateClosed` 빠른 경로 — 송신 배치당 lock 1회 제거

### 구현

송신 배치가 끝날 때마다 `OnSendEnd()`가 `ValidateClosed(...)`를 부르고, 그 메서드는
**무조건 `lock (SyncRoot)`** 를 잡았다. 그런데 닫힘 절차 밖(평상시)에서는 락 안에서 하는 일이
없다. `InSending`을 내리는 `Interlocked.And`의 **반환값**으로 닫힘 여부를 판정해,
닫힘 절차 밖이면 `ValidateClosed`를 아예 호출하지 않게 했다
(`RemoveStateFlagAndGetState` 헬퍼 추가).

### 안전성 논거

우려는 "`Close()`가 송신 중이라 소켓을 닫지 않고 반환했는데 송신 경로도 빠른 경로로 빠져
아무도 닫지 않는" 누락이다. `_state`의 모든 전이가 같은 변수에 대한 Interlocked 연산이라
**전역 전순서**가 있고, `Close()`는 ① `InClosing` 세트 → ② `InSending` 관찰 순서다.

- Close가 ②에서 `InSending`이 켜져 있음을 봤다면, 우리의 And는 그 관찰 이후이므로 And의
  반환값에 ①의 `InClosing`이 반드시 포함된다 → 느린 경로로 가서 기존과 동일하게 닫는다.
- Close가 ②에서 꺼져 있음을 봤다면 Close 자신이 `InternalClose`를 수행한다 → 누락 없음.

판정을 **And의 반환값**으로 하는 것이 핵심이다. 이후의 일반 read로 판정하면 논증이 더 약한
성질에 기대게 된다. 또한 기존에도 닫힘 절차 밖의 `ValidateClosed(..., forSend:true)`는
`closeReason`을 버리고 아무 일도 하지 않았으므로(타임아웃 사유 포함) 동작이 보존된다.

### 검증

- `Closing a session while sends are draining fires the close exactly once`:
  요청 핸들러가 32건을 큐에 넣은 **직후** `Close()`를 호출해 `InSending`이 켜진 상태에서
  닫힘이 걸리도록 만들고, 이를 150라운드 반복한다. 종료 이벤트가 라운드당 정확히 1회
  발생하고 접속 거부가 0건인지 확인한다.
- 이 테스트는 라운드마다 세션이 완전히 사라질 때까지 기다린다. 클라이언트가 소켓 종료를
  관측하는 시점이 서버의 세션 정리보다 조금 빨라서, 곧바로 재접속하면 종료 자체가 아니라
  접속 한도만 시험하게 되기 때문이다(초기 작성 시 실제로 이 함정에 빠졌다).

---

## P3. 리스너 병렬 accept 루프 (opt-in)

### 구현

- `ServerConfig.AcceptLoopCount` 추가, **기본값 1 = 기존 동작과 완전히 동일**.
  `1..64`로 클램프한다(`DefaultAcceptLoopCount` / `MaxAcceptLoopCount` 상수).
  `KeepAliveRetryCount`와 같은 패턴으로 `ServerConfig`에만 두어 `IServerConfig`는 불변이다.
- `TcpAsyncSocketListener.Start`가 루프를 N개 띄운다. 동일 리슨 소켓에 대한 동시
  `AcceptAsync`는 지원되는 방식이며, Windows에서 accept 경로를 넓히는 표준 수단이다.
- `OnStopped()` 1회 보장은 `_runningAcceptLoops` 카운트다운으로 한다(마지막 루프만 발생시킴).

accept 자체보다 그 뒤에 이어지는 연결당 작업(소켓 옵션 8회, 세션 생성·등록)이 직렬화되던 것이
실제 이득 지점이다. 정상 트래픽에는 효과가 없고, 재접속 폭주에만 의미가 있다.

### 검증

- `Parallel accept loops register every connection of a burst`: `AcceptLoopCount=4`로
  64개 동시 접속 → 전부 세션 등록 확인 → 각각 실제 에코 왕복까지 확인.
- `An out-of-range accept loop count is clamped into range`: `AcceptLoopCount=0`이어도
  정상 동작(클램프가 없다면 루프 0개 = 아무것도 accept하지 못한다).
- 기본값이 1인지도 단언한다.

---

## P4. 유휴 세션 zero-byte receive (opt-in)

### 구현

- `ServerConfig.UseZeroByteReceive` 추가, **기본값 false = 기존 동작과 완전히 동일**.
- `StartReceive`: 켜져 있고 대기 중이면 `e.SetBuffer(Memory<byte>.Empty)`로 빈 수신을 건다.
  파이프에서 버퍼를 빌리지 않으므로 **트래픽 없는 세션은 수신 버퍼를 전혀 점유하지 않는다.**
  데이터가 왔다는 것이 확인되면 그때 실제 버퍼를 받아 진짜 수신을 건다.
- `ProcessReceiveCore`: 완료된 수신의 **게시한 버퍼 길이가 0**이면 프로브로 판정하고,
  `BytesTransferred==0`을 종료로 해석하지 않는다. 종료 판별은 뒤따르는 실수신이 그대로 한다.

프로브 판정을 별도 플래그가 아니라 **SAEA에 게시한 버퍼 길이**로 하는 것이 요점이다.
게시한 스레드와 완료를 처리하는 스레드 사이에 공유 상태를 두지 않아도 되기 때문이다.

### 트레이드오프 (반드시 인지할 것)

데이터 버스트마다 수신 호출이 1회 늘어난다(프로브 + 실수신). **트래픽이 조밀한 서버에는
손해**다. 유휴 세션 비중이 큰 서버 전용 옵션이며, 설정 주석에도 그렇게 적었다.

### 검증

옵션을 켠 상태로 4건: 800패킷 에코 순서 보존 / 1.5초씩 유휴 후 재개 2회 /
클라이언트 종료 감지 / 수신 버퍼(2048)보다 훨씬 큰 20KB 페이로드 20건의 바이트 단위 일치.
기본값이 false인지도 단언한다.

---

## P5. 마이크로 묶음

1. **LingerOption 캐시**: 연결마다 `new LingerOption(false, 0)`을 할당하던 것을
   `static readonly` 1개로 대체(`SetSocketOption`은 값만 읽으므로 공유 안전).
2. **부분 송신 로그 가드**: `IsInfoEnabled` 가드를 씌워 문자열 보간이 무조건 실행되지 않게 했다.

3번(문자열 Send의 풀 인코딩)은 계획대로 하지 않았다. 게임 서버(바이너리 프로토콜)에는 효과가
없고 문자열 프로토콜 사용자에게만 의미가 있어 우선순위가 낮다.

---

## 측정

측정 환경은 16코어 Windows 11, loopback이다. **클라이언트와 서버가 같은 16코어를 나눠 쓰므로**
서버 단독 성능보다 개선 폭이 축소되어 보인다는 점을 감안해야 한다.

### 포화 조건 확보

처음 잡은 기준선(200 클라이언트, `SendRate 5`)은 목표 1,000 pps에 달성률 0.967로
**서버가 포화되지 않은** 조건이었다. 이 상태에서는 카운터 경합 개선이 드러날 수 없다.
`SendRate 300`(목표 60,000 pps)으로 올리자 실제 13,194 pps / 달성률 0.22가 되어
서버가 병목인 것이 확인됐고, 이 조건으로 비교했다.

### 비교 방법

원본 코드(HEAD)를 별도 git worktree에 체크아웃해 빌드하고, 개선본과 **교차로 번갈아**
3회씩 실행했다(열·백그라운드 드리프트가 한쪽에만 실리지 않게 하기 위함).
조건: 200 클라이언트 / 30초 / closed pacing / `SendRate 300`.

### 결과

처리량은 `steady_send_rate_per_sec`, 지연은 클라이언트 RTT다. pps는 3회 중앙값이고,
괄호 안은 3회 전부다(노이즈 폭을 보기 위함).

**1차 — HEAD vs 전체 변경(P1 포함)**

| 항목 | HEAD | 전체 변경 | 차이 |
|---|---|---|---|
| 처리량 pps | **13,347** (13,347 / 13,287 / 13,348) | **13,185** (13,176 / 13,185 / 13,241) | **-1.2%** |
| RTT p50 | 827 µs | 759 µs | -8% (개선) |
| RTT p99 | 3,007 µs | 2,911 µs | -3% (개선) |

처리량은 두 그룹의 범위가 겹치지 않는다(HEAD 최저 13,287 > 변경본 최고 13,241).
노이즈가 아니라 실재하는 손실이다.

**2차 — HEAD vs P1만 제외한 변경**

| 항목 | HEAD | P1 제외 | 차이 |
|---|---|---|---|
| 처리량 pps | **13,262** (13,291 / 13,262 / 13,180) | **13,248** (13,312 / 13,248 / 13,213) | -0.1% (구분 불가) |
| RTT p50 | 731 µs | 727 µs | 동일 |
| RTT p99 | 3,087 µs | 3,055 µs | -1% (동일 수준) |

범위가 완전히 중첩한다(HEAD 13,180~13,291, P1 제외 13,213~13,312). 두 실행 모두
타임아웃 0, 소켓 오류 0이다.

### 해석

- **1.2% 손실의 원인은 P1**이다. 빼면 사라진다 → P1 철회.
- **최종본(P2~P5 + B1)은 정상 부하에서 기존 코드와 측정상 구분되지 않는다.** 즉 이번 작업은
  처리량을 올리지 못했고, 대신 아무것도 잃지 않았다.
- P2가 제거한 것은 배치당 비경합 모니터 1회(수십 ns)로, 이 부하(13k pps)에서는 애초에
  측정 한계 아래다. 기대대로 "손해는 아니지만 눈에 띄는 이득도 없음"으로 나왔다.
- 반복 간 노이즈 폭은 약 ±0.5%다. 이보다 작은 차이는 이 장비·이 방법으로는 판정할 수 없다.

### 이 측정의 한계 (중요)

- 클라이언트와 서버가 **같은 16코어**를 나눠 쓴다. 서버 단독 성능이 아니며, 코어 경합에서
  오는 개선(P1이 노렸던 것)은 특히 관측되기 어렵다.
- 그래서 이 장비의 상한이 약 13,200 pps다. 전용 부하 장비 + 전용 서버 장비 구성이 되면
  P1을 포함해 재평가할 가치가 있다.
- P3(접속 폭주)와 P4(유휴 세션 메모리)는 **이 시나리오가 겨냥하는 상황이 아니다.**
  각각 재접속 폭주 시나리오와 대량 유휴 세션 시나리오로 따로 측정해야 의미가 있고,
  이번에는 "기본값에서 회귀가 없다"까지만 확인했다.

---

## 검토했지만 하지 않기로 한 것들 (이유 포함)

| 후보 | 기각 이유 |
|---|---|
| **세션/AppSession/Pipe 풀링(재사용)** | 최대 효과 후보지만 안정성 비용이 정확히 반대급부. 앱이 파생한 `AppSession` 서브클래스의 필드, 필터 인스턴스 상태, 이벤트 구독이 세션 간에 누출될 수 있고, 이는 라이브러리가 통제할 수 없는 앱 코드 계약 파괴다. "안정성을 해치지 않으면서"라는 전제와 정면 충돌 → 기각. |
| **PipeScheduler.Inline (reader 인라인 실행)** | FlushAsync가 IOCP 스레드에서 앱 요청 핸들러까지 실행하게 됨 → 수신 정지·데드락 위험. `ReceiveInlineOnIocpThread`가 "파이프 전진까지만 인라인"으로 선을 그은 설계 의도와 충돌 → 기각. |
| **ChannelSendingQueue를 수제 lock-free 큐로 교체** | 세션당 큐라 경합이 거의 없고, 송신 1회의 syscall(µs) 대비 수십 ns 수준. `EnqueueAsync`(대기·취소·완료 의미) 재구현 위험 대비 이득이 없다 → 기각. |
| **SessionID를 GUID 문자열이 아닌 값으로** | 접속당 할당 2회가 아깝지만 `SessionID: string`은 공개 계약이고 앱이 포맷에 의존할 수 있다 → 기각. |
| **`_sessionDict` 비교자 OrdinalIgnoreCase → Ordinal** | 이득이 너무 작아 호환성 리스크를 정당화하지 못한다 → 기각. |
| **송신 배치 코얼레싱** | scatter-gather(`BufferList`)가 이미 세그먼트 N개를 syscall 1회로 보낸다. 복사를 더하면 손해 → 기각. |
| **UDP 경로 최적화** | 주 사용처가 TCP이고 UDP는 LoadTest 커버리지가 없다. 측정 없는 최적화는 이 프로젝트 원칙 밖 → 보류. |
| **RIO/io_uring 등 커널 레벨 재작성** | 플랫폼 종속 대규모 재작성. 범용 .NET 소켓 서버라는 성격과 불일치 → 기각. |
