# SuperSocketLite2.GameServerTemplate

이 프로젝트의 규칙은 `AGENTS.md` 에 있다.

@AGENTS.md

SuperSocketLite2 로 코드를 쓰거나 고칠 때는 `supersocketlite2` 스킬을 쓴다
(`.claude/skills/supersocketlite2/`). 저장소에 함께 들어 있으므로 따로 설치할 필요가 없다.
